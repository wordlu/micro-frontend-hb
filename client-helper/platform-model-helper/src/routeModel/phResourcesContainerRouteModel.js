// eslint-disable-next-line no-unused-vars
export async function phResourcesContainerRouteModel(route, parseParams) {
    // let resourcesList = results[0].filter((it) => it)
    return {
        _isVue: true
    }
}
