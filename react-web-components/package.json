{
	"name": "root",
	"version": "1.0.0",
	"main": "index.js",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build:example": "yarn workspace @pharbers/react-example-components build",
		"clean": "find . -name 'node_modules' -type d -prune -print -exec rm -rf '{}' \\;"
	}
}
